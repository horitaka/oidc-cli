package constants

const (
	SERVER_BASE_URL = "http://localhost"
	PORT            = "8080"
	LOGIN_URL       = SERVER_BASE_URL + ":" + PORT + "/login"
	CALLBACK_URL    = SERVER_BASE_URL + ":" + PORT + "/callback"
)
